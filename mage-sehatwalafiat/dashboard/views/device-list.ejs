<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pilih Perangkat - SEHATI</title>
  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkPKZ5QiAj6Ta86w+fsb2TkcmfRyVX3pBnMFcV7oQPJkl9QevSCWr3W6A==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body {
      background-color: #f4f7f6;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
    }
    .device-selector {
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.1);
      padding: 2rem;
      width: 90%;
      max-width: 600px;
    }
    .device-selector h1 {
      text-align: center;
      color: #0d6efd; /* [REVISI] Menggunakan warna primary Bootstrap */
      margin-top: 0;
      margin-bottom: 2rem;
      font-weight: bold;
    }
    .device-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    .device-item a {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1.25rem 1.5rem;
      border: 1px solid #ddd;
      border-radius: 6px;
      margin-bottom: 1rem;
      text-decoration: none;
      color: #333;
      transition: background-color 0.2s, box-shadow 0.2s;
    }
    .device-item a:hover {
      background-color: #f9f9f9;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
      border-color: #0d6efd;
    }
    .device-info {
      display: flex;
      align-items: center;
    }
    .device-info .icon {
      font-size: 1.5rem;
      color: #0d6efd;
      margin-right: 1rem;
    }
    .device-info span {
      font-size: 1.2rem;
      font-weight: 500;
    }
    .device-status {
      font-size: 0.9rem;
      padding: 0.25rem 0.75rem;
      border-radius: 12px;
      font-weight: bold;
    }
    .device-status.online {
      background-color: #d4edda;
      color: #155724;
    }
    .device-status.offline {
      background-color: #f8d7da;
      color: #721c24;
    }
  </style>
</head>
<body>
  <div class="device-selector">
    <h1><i class="fa-solid fa-heart-pulse"></i> SEHATI Monitoring</h1>
    <h3 class="h4 text-center text-muted mb-4">Pilih Perangkat untuk Dipantau</h3>
    
    <ul class="device-list">
      
      <% if (typeof devices !== 'undefined' && devices.length > 0) { %>
        <% devices.forEach(device => { %>
          
          <li class="device-item">
            
            <a href="/device/dispatch/<%= device.id %>">
              
              <div class="device-info">
                <i class="icon fa-solid fa-microchip"></i>
                <span><%= device.device_name %></span>
              </div>
              
              <% const statusClass = device.status === 'online' ? 'online' : 'offline'; %>
              <span class="device-status <%= statusClass %>">
                <%= device.status %>
              </span>
              
            </a>
          </li>
          
          <% }); %>
          <% } else { %>
            <li class="list-group-item text-center text-muted">Tidak ada perangkat terdaftar.</li>
            <% } %>
            
          </ul>
        </div>
      </body>
      </html>